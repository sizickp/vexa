{
  "name": "@vexa/telemost-capture",
  "version": "0.1.0",
  "description": "Yandex Telemost capture (browser): speaking-participant watcher over the call's participant tiles (→ mixed-capture.v1 hints). Mixed audio from @vexa/mixed-capture-core; names resolve downstream.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "tsx src/telemost-capture.test.ts",
    "check:isolation": "node scripts/check-isolation.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
