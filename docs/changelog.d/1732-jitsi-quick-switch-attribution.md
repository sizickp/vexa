- **Jitsi: a quick reply now gets its own row under its own name (#1732).** In a lively exchange the
  first seconds of a reply landed in the previous speaker's row, and short replies right after a
  switch published as "Speaker". Jitsi's dominant-speaker hint now cuts the turn at the takeover, the
  quick-reply guard built for noisy tiles no longer holds Jitsi rows provisional, and a turn is named
  over the audio it actually transcribed. Measured on a two-person Jitsi call: named rows 87.7% → 100%
  with no rise in misattribution.
